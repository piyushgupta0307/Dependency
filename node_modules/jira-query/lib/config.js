'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getConfig = getConfig;
exports.setConfig = setConfig;

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var fileName = '.jira-queryrc';
var manuallySetConfig = null;

function getHomePath() {
  return process.env[process.platform === 'win32' ? 'USERPROFILE' : 'HOME'];
}

function readConfigInDir(dir) {
  return _fs2.default.readFileSync(_path2.default.join(dir, fileName), 'utf8');
}

function getConfigFile() {
  try {
    return readConfigInDir(process.cwd());
  } catch (err) {
    return readConfigInDir(getHomePath());
  }
}

function getConfig() {
  if (manuallySetConfig) {
    return manuallySetConfig;
  }

  var config = null;

  try {
    var file = getConfigFile();

    try {
      config = JSON.parse(file);
    } catch (err) {
      console.log('invalid json in ' + fileName);
    }
  } catch (err) {
    // console.log('no rc');
  }

  return config;
}

function setConfig(config) {
  manuallySetConfig = config;
}